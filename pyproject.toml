[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nvoip-python"
version = "0.1.0"
description = "Cliente Python simples para a API v2 da Nvoip."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
